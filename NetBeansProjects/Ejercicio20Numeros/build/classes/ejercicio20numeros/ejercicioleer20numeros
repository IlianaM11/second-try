
package ejercicio20numeros;
import java.util.Scanner;
/**
 *
 * @author nissi
 */
public class Ejercicio20Numeros {
/*Escriba un programa que lea 20 números. Si el número leído es igual a cero se debe salir del
bucle y mostrar el mensaje "Se capturó el numero cero". El programa deberá calcular y mostrar el
resultado de la suma de los números leídos, pero si el número es negativo no debe sumarse.
Nota: recordar el uso de la sentencia break.
    
    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
         Scanner reader = new Scanner(System.in);
         
         int Numeros;
         int suma=0;
         int contador=1;
         
         do{
         System.out.println("Ingrese  Numeros: "); 
         Numeros = reader.nextInt();
         System.out.println(contador+ ". " + Numeros);
         contador = contador + 1;
         suma = suma + Numeros;
         System.out.println("El Total de los numeros leidos es: " + suma);
          
          if ((Numeros==0)) {
           System.out.println("Se capturo el Numero 0 ");
            break;
         }
         
         if ((Numeros<0)) {
                System.out.println("El numero es Negativo ");
                break;  
            }
         
         if ((contador>20)) {
                System.out.println();
                break;  
            }
         
          }while(!(Numeros>=0) || !(Numeros<0) || !(contador>20));
         
         
         
          //for (int i =1 ; i <= 20 ; i++) {
            //System.out.println("* " + i);
            //Contador = Contador + i; 
            
            
              
        }
          
    }
   

